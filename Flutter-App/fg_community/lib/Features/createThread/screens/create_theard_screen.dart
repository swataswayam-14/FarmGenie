import 'package:flutter/material.dart';

class CreateTheardScreen extends StatelessWidget {
  const CreateTheardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Thread'),
      ),
      body: Center(
        child: Text('Coming Soon...'),
      ),
    );
  }
}
